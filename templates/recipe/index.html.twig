{% extends 'base.html.twig' %}

{% block title "Toutes les recettes" %}

{% block body %}
    <p>Lorem ipsum dolor sit amet, consectetur adipiscing elit. Sed porta ligula eget est vulputate bibendum. Etiam augue
    nibh, blandit volutpat lacus ac, efficitur cursus neque. Vivamus rhoncus facilisis tortor eu volutpat. Morbi euismod
    justo nec mollis tincidunt. Suspendisse a cursus lacus. Quisque vel consectetur augue, id auctor mi. Nullam dictum
    vulputate justo a hendrerit. Mauris varius arcu ac enim lobortis tincidunt in quis turpis.</p>
    <p>
        <a class="btn btn-primary" href="{{ path('recipe.add') }}">Créer une recette</a>
    </p>
    <table class="table">
        <thead>
            <tr>
                <th>Titre</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
            {% for recipe in recipes %}
            <tr>
                <td>
                    <a href="{{ path('recipe.show', {id: recipe.id, slug: recipe.slug }) }}">{{ recipe.title }}</a>
                </td>
                <td>
                    <div class="d-flex gap-1">
                        <a class="btn btn-primary btn-sm" href="{{ path('recipe.edit', {id: recipe.id}) }}">Modifier</a>
                        <form action="{{ path('recipe.delete', {id: recipe.id}) }}" method="post">
                            <input type="hidden" name="_method" value="DELETE">
                            <button type="submit" class="btn btn-danger btn-sm">Supprimer</button>
                        </form>
                    </div>
                </td>
            </tr>
            {% endfor %}
        </tbody>
    </table>
{% endblock %}